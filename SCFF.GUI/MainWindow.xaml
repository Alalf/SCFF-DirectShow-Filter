<Window x:Class="SCFF.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="http://schemas.microsoft.com/winfx/2006/xaml/presentation/shell"
        Title="SCFF DirectShow Filter Ver.0.1.7" Height="445.274" Width="524.008" SnapsToDevicePixels="True" Background="#FF333333" Icon="Images/app.ico" ResizeMode="CanResizeWithGrip" Name="mainWindow">
  <Window.CommandBindings>
    <CommandBinding Command="{x:Static shell:SystemCommands.CloseWindowCommand}"
                    Executed="closeCommand_Executed"/>
    <CommandBinding Command="{x:Static shell:SystemCommands.MaximizeWindowCommand}"
                    Executed="maximizeWindow_Executed"/>
    <CommandBinding Command="{x:Static shell:SystemCommands.MinimizeWindowCommand}"
                    Executed="minimizeWindow_Executed"/>
    <CommandBinding Command="{x:Static shell:SystemCommands.RestoreWindowCommand}"
                    Executed="restoreWindow_Executed"/>
  </Window.CommandBindings>
  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="{x:Static SystemParameters.CaptionHeight}" ResizeBorderThickness="8" GlassFrameThickness="0,0,0,1" />
  </shell:WindowChrome.WindowChrome>
  <Border BorderBrush="#FF3F3F46" BorderThickness="1">
    <DockPanel LastChildFill="False" Margin="0,0,0,0">
      <Grid DockPanel.Dock="Top" Height="{x:Static SystemParameters.CaptionHeight}">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="21"/>
          <ColumnDefinition/>
          <ColumnDefinition Width="65"/>
        </Grid.ColumnDefinitions>
        <Rectangle Width="11" Height="11" Fill="#FF003B00" Margin="5"/>
        <Label Grid.Column="1" Foreground="#FFA0A0A0" FontSize="11" Content="SCFF DirectShow Filter Ver.0.1.7" VerticalAlignment="Center" Margin="0,2,0,3"/>
        <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
          <StackPanel.Resources>
            <ResourceDictionary Source="Resources\CaptionButton.xaml"/>
          </StackPanel.Resources>
          <Button Content="0" Style="{DynamicResource CaptionButton}" Command="{x:Static shell:SystemCommands.MinimizeWindowCommand}" CommandParameter="{Binding ElementName=mainWindow}"/>
          <Button Content="1" Style="{DynamicResource CaptionButton}" Command="{x:Static shell:SystemCommands.MaximizeWindowCommand}" CommandParameter="{Binding ElementName=mainWindow}"/>
          <Button Content="2" Style="{DynamicResource CaptionButton}" Command="{x:Static shell:SystemCommands.RestoreWindowCommand}" CommandParameter="{Binding ElementName=mainWindow}"/>
          <Button Content="r" Style="{DynamicResource CaptionButton}" Command="{x:Static shell:SystemCommands.CloseWindowCommand}" CommandParameter="{Binding ElementName=mainWindow}"/>
        </StackPanel>
      </Grid>
      <Menu DockPanel.Dock="Top" FontSize="11">
        <MenuItem Header="File(_F)">
          <MenuItem Header="Open profile(_O)..."/>
          <MenuItem Header="Save profile(_S)"/>
          <MenuItem Header="Save profile as(_a)..."/>
          <MenuItem Header="Exit(_X)" Command="{x:Static shell:SystemCommands.CloseWindowCommand}" CommandParameter="{Binding ElementName=mainWindow}"/>
        </MenuItem>
        <MenuItem Header="View(_V)">
          <MenuItem Header="Minimum(_M)"/>
          <MenuItem Header="Native Layout(_N)"/>
          <MenuItem Header="Complex Layout(_C)"/>
        </MenuItem>
        <MenuItem Header="Options(_O)">
          <MenuItem Header="Force Aero ON(_A)"/>
          <MenuItem Header="Restore last profile(_R)"/>
        </MenuItem>
      </Menu>
      <TabControl TabStripPlacement="Bottom" DockPanel.Dock="Left" FontSize="9">
        <TabItem Header="1">
          <StackPanel Width="300">
            <GroupBox Header="Target" FontSize="9">
              <Grid Height="130">
                <Grid.RowDefinitions>
                  <RowDefinition Height="23*"/>
                  <RowDefinition Height="42*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="45*"/>
                  <ColumnDefinition Width="64*"/>
                  <ColumnDefinition Width="37*"/>
                </Grid.ColumnDefinitions>
                <Label HorizontalAlignment="Center" VerticalAlignment="Center" Height="18" Width="44">Caption</Label>
              </Grid>
            </GroupBox>
            <Expander Header="Area" IsExpanded="True">
              <Grid Height="130" Background="#FF333333">

              </Grid>
            </Expander>
          </StackPanel>
        </TabItem>
        <TabItem Header="2"></TabItem>
        <TabItem Header="3"></TabItem>
        <TabItem Header="4"></TabItem>
        <TabItem Header="5"></TabItem>
        <TabItem Header="6"></TabItem>
        <TabItem Header="7"></TabItem>
        <TabItem Header="8"></TabItem>
      </TabControl>
    </DockPanel>
  </Border>
</Window>
